export interface Atmmodel {
    // atm-card.model.ts

    userid: number;
    familyid:number;
    number: number;
    entityname: string;
    issueDate: string; // Change to Date if necessary
    expireDate: string; // Change to Date if necessary
    username: string;
    cvv: number;
  }
  

